#
# Edit this file and rename it to "secrets.yaml"
#

lights:
  bases:

  - name: my-hue-base

    # Only `hue` is supported now
    type: hue

    address: https://10.1.2.3

    # With hue, the key is the "username"
    key: "ajfhandfhmadhfx"

    
